#include "GameSetting.h" // Include its own header for consistency checks

// Define the global constant values
const float SCREEN_WIDTH = 1920;
const float SCREEN_HEIGHT = 1080;
const unsigned int VERTICAL_BLOCKS_COUNT = 24;
const float BLOCK_SIZE = SCREEN_HEIGHT / VERTICAL_BLOCKS_COUNT;